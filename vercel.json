{
  "buildCommand": "curl https://sh.rustup.rs -sSf | sh -s -- -y && source $HOME/.cargo/env && rustup target add wasm32-unknown-unknown && cargo install trunk --locked && trunk build --release",
  "outputDirectory": "dist",
  "installCommand": "echo 'skipping npm install'"
}
